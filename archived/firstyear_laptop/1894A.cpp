#include<bits/stdc++.h>

using namespace std;
const int maxN = 1e3+5;
int n, c[2];
string s;
int check(int x, int y){
	for (int i = 0; i < n; i++){
			
	}
} 
void solve(){
	cin >> n >> s;
	c[0] = c[1] = 0;
	cout << s.back() << "\n";
}
int main(){
	ios::sync_with_stdio(0);
	cin.tie(0); cout.tie(0);
	int t; cin >> t;
	while (t--)
	solve();
	return 0;
}
